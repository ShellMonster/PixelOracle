/**
 * 项目配置常量
 * 包含图片检测、UI尺寸、超时配置、存储限制等常量定义
 */

export const CONFIG = {
  // ========== 图片检测配置 ==========
  /** 最小图片尺寸（像素），小于此尺寸的图片将被忽略 */
  IMAGE_MIN_SIZE: 64,

  // ========== UI尺寸配置 ==========
  /** 按钮尺寸（像素） */
  BUTTON_SIZE: 28,
  /** 按钮偏移量（像素），用于定位按钮位置 */
  BUTTON_OFFSET: 4,
  /** 图标尺寸（像素） */
  ICON_SIZE: 16,
  /** 弹窗宽度（像素） */
  POPUP_WIDTH: 320,
  /** 弹窗最大高度（像素） */
  POPUP_MAX_HEIGHT: 400,
  /** 弹窗边距（像素） */
  POPUP_MARGIN: 8,

  // ========== 缩略图配置 ==========
  /** 缩略图最大尺寸（像素），用于限制预览图大小 */
  MAX_THUMBNAIL_SIZE: 256,

  // ========== 超时配置（毫秒） ==========
  /** 默认请求超时时间（2分钟） */
  DEFAULT_TIMEOUT_MS: 120000,
  /** 消息通信超时时间（2分10秒） */
  MESSAGE_TIMEOUT_MS: 130000,
  /** Tab消息超时时间（30秒） */
  TAB_MESSAGE_TIMEOUT_MS: 30000,
  /** 防抖延迟时间（毫秒），用于优化频繁操作 */
  DEBOUNCE_MS: 100,

  // ========== 存储限制配置 ==========
  /** 历史记录最大保存条数 */
  MAX_HISTORY_ITEMS: 50,
} as const
